[project]
name = "spatialdata-test-fixtures-v0.7.0"
version = "0.1.0"
description = "Test fixture generation for SpatialData.ts using spatialdata 0.7.0"
requires-python = ">=3.12"
dependencies = [
    "spatialdata==0.7.0",
]

